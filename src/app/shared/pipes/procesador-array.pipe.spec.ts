import { ProcesadorArrayPipe } from './procesador-array.pipe';

describe('ProcesadorArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcesadorArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
