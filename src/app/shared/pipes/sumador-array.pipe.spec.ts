import { SumadorArrayPipe } from './sumador-array.pipe';

describe('SumadorArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SumadorArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
