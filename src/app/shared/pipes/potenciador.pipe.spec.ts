import { PotenciadorPipe } from './potenciador.pipe';

describe('PotenciadorPipe', () => {
  it('create an instance', () => {
    const pipe = new PotenciadorPipe();
    expect(pipe).toBeTruthy();
  });
});
